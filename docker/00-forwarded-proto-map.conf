# Incluído no contexto http (conf.d). Terminação TLS no Apache/Nginx do host
# envia X-Forwarded-Proto; aqui repassamos ao backend sem perder o valor real.
map $http_x_forwarded_proto $esquema_cliente {
    default $http_x_forwarded_proto;
    ""      $scheme;
}
